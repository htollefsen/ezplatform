language: php

matrix:
    include:
        - php: 5.5
          env: deps=high
        - php: 5.6
          env: deps=high
        - php: 7.0
          env: deps=high
        - php: hhvm
          env: deps=high
        - php: 5.5
          env: deps=low
        - php: 5.6
          env: deps=low
        - php: 7.0
          env: deps=low
        - php: hhvm
          env: deps=low
    allow_failures:
        - php: 5.5
          env: deps=low
        - php: 5.6
          env: deps=low
        - php: 7.0
          env: deps=low
        - php: hhvm
          env: deps=low

cache:
    directories:
        - vendor
        - $HOME/.composer/cache

before_install:
    - composer self-update -q

install:
    - if [ -z "$deps" ]; then composer install; fi;
    - if [ "$deps" = "low" ]; then composer update --prefer-lowest --no-interaction; fi;
    - if [ "$deps" = "high" ]; then composer update --no-interaction; fi;

script:
    - vendor/phpunit/phpunit/phpunit

